{
    "builds":[
        {
            "src":"./server/index.js",
            "use":"@vercel/node",
            "config": {
           "distDir": "build"
            }
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"./server/index.js"
        }
    ]
}
